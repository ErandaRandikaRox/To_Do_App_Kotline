<?xml version="1.0" encoding="utf-8"?>
<!--
    Splash screen layout for the To-Do app.
    Displays an app logo and title in the center of the screen.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:background="@color/white"
    android:orientation="vertical">

    <!--
        ImageView to display the app logo.
        Positioned at the center of the screen.
    -->
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_launcher_foreground"
        android:contentDescription="Splash Logo" />

    <!--
        TextView to display the app name "To-Do App".
        Uses bold styling with a font size of 24sp.
    -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="To-Do App"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:layout_marginTop="16dp"/>
</LinearLayout>
